# v2.10 Release Notes Archive

> Source: [CHANGELOG.md](../../CHANGELOG.md)

v2.10 cycle 主題：cross-repo refresh + tool description/title 重構 + 13 項
backlog item。Tool count 181 → 180（淨 -1），整體 LOC 縮減 ~1500 行。
6 個 patch (`v2.10.0` 到 `v2.10.5`) — landmine 文件級結案 + 5 個 stage。

委派軌跡：codex CLI（gpt-5.5 / codex-cli 0.128.0）做 stage 1 和 stage 4 #13；
gemini-3.1-pro-preview 做 stage 2/3 大部分；我手補小缺口與品質清理。

---

## v2.10.5 — 2026-05-03

Cycle wrap 後 backlog 驗證審計，補 3 個小缺口。

### 🟡 補完 v2.10.x 列出但未到位的 3 件

1. **`TiledMapInterpreter` 加入 asset-interpreters**（importerType `tiled-map`）。
   v2.10.3 #3 補 7/8 user 列出的 interpreter，Tiled 漏了。
   現在 16 specialized + 1 Unknown fallback = 17 classes（>=15 目標達標）。
2. **`source/lib/schemas.ts` 補 `referenceSchema`** — 為原 spec 列出但 v2.10.2 #11
   Polish A 提取時漏掉。改為從 `instance-reference.ts` re-export 統一進入點。
3. **`source/tools/node-tools.ts` 移除 inline `vec3Schema`**，改 import from
   `lib/schemas`。v2.10.2 #11 建立共用 schema 但 inline 重複定義仍在。

未處理（明確延後）：
- Decorator unification（16 檔轉 `@mcpTool`）：effort 高、改動面廣，延 v3.0。
- 中型檔案 narrative description 外移（6 檔）：Polish B 明確排除，延 v2.11。

---

## v2.10.4 — 2026-05-03

Stage 4 wrap — preferences macro / discover prompt template / Polish C +
v2.10.2 #11 / v2.10.3 #12 leftover 品質清理。

### Stage 4 sub-tasks

- **`preferences_manage` macro**（#8，commit `6c38bbb`）：preferences 7 tool 收成
  1 個 op-router macro，跟隨 v2.9.3 referenceImage 範式。Schema flat 而非
  `z.discriminatedUnion`（landmine #15 — Gemini-compat `oneOf` 風險）。
  Tool count 186 → 180（淨 -6）。
- **`discover_then_act_workflow` prompt template**（#9，commit `b1fd2c0`）：
  cocos-code-mode 三步式落地 prompt。配合 v2.5 已 ship 的 prompts framework。
  Step 1 query tree → Step 2 inspector_get_instance_definition → Step 3 set 屬性。
- **Polish C — 移除冗餘 try/catch**（#13，commit `f8e01cf`）：codex CLI direct
  審查 8 tool 檔，移除 inner try/catch ONLY produce identical
  `fail(err.message)` 的 case（外層 `defineTools` 已包同樣 envelope）。
  -150 LOC source。Protected: scene.ts / scene-bridge.ts / define-tools.ts 不動。

### Quality polish for #11/#12 leftovers

- **scene.ts 54 個 envelope literal 補完**（gemini-2.5-flash phase 1 stop early）：
  perl + Node multi-line regex pass + 1 manual edit for `capturedLogs` spread case。
  全部遷移到 `ok()/fail()`。
- **node-classifications.ts 嚴格 Set lookup**（移除 `type.includes(comp)` 部分匹配
  fallback）：理論上 `cc.SpriteFrame` 會誤命中 `cc.Sprite`，cocos 命名雖嚴格但
  保險改 `BUILTIN_2D_COMPONENTS.has(type)` 嚴格判定。

---

## v2.10.3 — 2026-05-03

Stage 3 wrap — 5 sub-task 全 ship（每個獨立 commit）。

- **#3 asset-interpreters 8 → 15**（commit `b339415`，gemini-3.1-pro-preview）：
  補 AnimationClip / AudioClip / Prefab / Scene / LabelAtlas / Spine / Json
  共 7 個 specialized interpreter。每個 delegate 到 `BaseAssetInterpreter`
  generic userData / subMetas extraction（保守設計，domain-specific override
  待具體需求再補）。assetMeta_list_interpreters 由 8 變 15。
- **#5 debug_record_* format/quality**（commit `d19482d`）：record_start schema 加
  `quality` (low=1M / medium=5M / high=12M) + `videoCodec` (vp8/vp9/h264/av1)
  + mp4↔webm fallback chain. GameDebugClient `MediaRecorder.isTypeSupported()`
  pre-check + warning 嵌入 ToolResponse.data。
- **#6 priority labeling**（commit `520d0c9`）：FunplayAI 對照 — `[primary]` 標
  `execute_javascript`，其餘 178 narrow tool 標 `[specialist]`。已有 tag
  （`[claude-code-redundant]` / `❌` / `⚠`）的 7 個保持不動。
- **#7 `get_users(uuid)` asset 反向參考**（commit `fb04001`）：assetAdvanced 新 tool。
  逐 scene/prefab/script 跑 query-asset-depends 找出 references。
  Tool count 185 → 186。
- **#12 Polish B 3 lib 抽取**（commit `c145950`，gemini-3.1-pro-preview）：
  `source/lib/cors.ts` (applyGameCorsHeaders + applyDefaultCorsHeaders) /
  `source/lib/log-parser.ts` (filterByLevel + filterByKeyword + searchWithContext) /
  `source/lib/node-classifications.ts` (BUILTIN_2D/3D_COMPONENTS + predicates)。
  Consumers (mcp-server-sdk / debug-tools / node-tools) 改用新 helpers。
  4th deliverable (scene-bridge envelope) skipped — gemini-3.1-pro-preview
  quota exhausted。

---

## v2.10.2 — 2026-05-03

Stage 2 wrap — 4 sub-task。

- **#1 TS class 定義生成擴充**（commit `34a4ecf`，codex gpt-5.5）：
  inspector-tools.ts renderTsClass 從 node-only 擴成支援 component dump +
  `@property({min,max,unit,radian,multiline,tooltip,type})` decorator +
  enum/BitMask hoisting + nested struct types。
  關閉 Discover-then-act 三步式 Step 2 gap（cross-repo-survey #1）。
- **#2 debug_game_command sub-action**（commit `a60a88a`，codex gpt-5.5）：
  GameDebugClient `state` (cc.director.getScene() dump) / `navigate`
  (customCommands.navigate hook) / `inspect` 強化（UITransform / Widget
  / Layout 拉取）。debug-tools.ts schema 變動 +3/-3，主要動 client template。
- **#4 animation-tools +4**（commit `0189086`，codex gpt-5.5）：
  list_animation_states / get_animation_state_info / set_animation_speed /
  check_animation_finished。Scene-side method registered in
  package.json contributions.scene.methods。Tool count 181 → 185。
- **#11 Polish A**（commit `c2c2ef6`，gemini-2.5-flash + gemini-3.1-pro-preview）：
  - `source/lib/response.ts` 新 `ok(data?, message?)` / `fail(error, data?)` helpers
  - `source/lib/schemas.ts` 抽 vec2/3/4 + colorSchema
  - 188 個 ToolResponse-envelope return literal 移轉到 helpers，跨 19 檔。
    AST-aware migration 保留 `warning`/`instruction`/`spread` 非標準字段。
  - 淨減 ~677 source LOC。

---

## v2.10.1 — 2026-05-03

Stage 1 wrap — tool description/title 重構（單一 sub-task）。

- **lib infra**：`source/lib/decorators.ts` + `source/lib/define-tools.ts` +
  `source/types/index.ts` 加可選 `title?: string` 欄位，串到
  `annotations.title`（per MCP spec 2025-03-26）。
  `source/mcp-server-sdk.ts` ListToolsRequestSchema 響應 forward annotations。
- **per-tool audit**（codex Stage 2 + 我手補 5 檔）：每個 tool 加 `title`
  (≤50 char verb-noun)，description 第一句 ≤120 char summary。
- **`scripts/generate-tools-doc.js` 重寫**：TOC 表加 title + 第一句 summary 欄；
  per-tool 區塊改為 `<details><summary>` 摺疊。tool count 從 registry 即時讀
  （不再硬寫 163/14）。
- **`docs/tools.md` 重生**：1493 → 3216 行（含 `<details>` 標記），181 對 181 平衡。

---

## v2.10.0 — 2026-05-02

Cycle 開頭 — landmine #16/#17 文件級結案 + cross-repo-survey 從
v2.2.0 baseline refresh 到 v2.9.7 baseline + v2.10 推進清單。

- **landmine #16/#17 結案**：cross-repo refresh 確認沒有 reference project
  ship 過可運作的 preview-mode setter / softReloadScene-safe preview start。
  - `debug_set_preview_mode` 改 `❌ NOT_SUPPORTED` 預設硬擋；`attemptAnyway`
    opt-in 才進診斷探測。
  - `debug_preview_control(start)` description 更新明指 embedded screenshot
    / game_command 兩條替代路徑，不再宣稱「pending future investigation」。
  - CLAUDE.md landmine 段同步更新。
- **`docs/research/cross-repo-survey.md`** 全面 refresh（v2.9.7 baseline）+ 加
  v2.10 推進清單 13 項 + Discover-then-act 狀態盤點 + LOC top-5 file 分析。
- **HANDOFF v2.10.x 推進清單**整合，分 4 stage。

---

## Cycle 統計

| 指標 | 起點 (v2.9.7) | 終點 (v2.10.5) | 變動 |
|---|---:|---:|---:|
| Tool 數 | 181 | 180 | -1（-7 prefs / +4 anim / +1 get_users / +1 prefs_manage） |
| Categories | 18 | 18 | 0 |
| Source LOC | 16,208 | ~14,700 | ~-1,500 |
| Lib 檔案 | (基礎) | +6 (response / schemas / cors / log-parser / node-classifications + 既有 scene-bridge etc.) | +6 |
| Asset-interpreters | 8 | 16 | +8 (Animation/Audio/Prefab/Scene/LabelAtlas/Spine/Json/Tiled) |
| MCP prompts | 4 | 5 | +1 (discover_then_act_workflow) |
| Gemini-compat | ✅ 181 inline | ✅ 180 inline | 維持 |

未做（延後）：
- **Decorator unification**（16 檔 inline → `@mcpTool`）：v3.0
- **中型檔案 narrative 外移**（6 檔，~500 LOC 收益）：v2.11+

完整 stage / per-sub-task 紀錄參見 `docs/archive/handoff/v2.10.md`。
